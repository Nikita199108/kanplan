"use client";

export default function Announcements() {
    return (
        <div
            aria-live="polite"
            aria-atomic="true"
            className="sr-only"
        >
            {}
        </div>
    );
}